h4 dogs
h4 {{id}}
h4{{bundle.name}}
h4 {{topicID}}

div(ng-repeat="bundle in bundles")
  md-button(ui-sref="{{bundle.id}}" layout layout-align="center center")
    h2 {{bundle.name}}
    h4 {{bundle.difficulty}}
    h4 DOGS


